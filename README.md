# 三层五子棋

## 运行

**最简单：** 双击 `开始游戏.bat`（会自动装依赖、开服务器、打开浏览器）。

或手动：

1. 安装 Node.js 18+
2. `npm install`
3. `npm start`
4. 主机与队友浏览器打开终端里打印的局域网地址
5. 一人「创建房间」，一人「加入房间」

关闭黑色命令行窗口即停止游戏。

## 说明

- 静态页面：`public/`
- 共享规则：浏览器通过 `/shared/*.js` 访问 `shared/`
- WebSocket 与 HTTP 共用同一端口（默认 3000）
